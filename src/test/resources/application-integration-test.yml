spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bancodigital_test
    username: bancodigital
    password: bancodigital
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  thymeleaf:
    cache: false

logging:
  level:
    root: WARN
    com.bancodigital: INFO
